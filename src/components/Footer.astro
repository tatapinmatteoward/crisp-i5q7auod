---
const currentYear = new Date().getFullYear();
const socialLinks = [
  { icon: 'fab fa-facebook', url: '#' },
  { icon: 'fab fa-instagram', url: '#' },
  { icon: 'fab fa-pinterest', url: '#' },
  { icon: 'fab fa-twitter', url: '#' }
];
---

<footer class="bg-[#cc5500] text-[#2e7d32] py-8">
  <div class="container mx-auto px-4 text-center">
    <p class="text-lg font-serif">&copy; {currentYear} Crispy Chronicles. All rights reserved.</p>
    <div class="flex justify-center gap-6 mt-4">
      {socialLinks.map((link) => (
        <a href={link.url} class="text-xl hover:text-white transition-colors duration-300">
          <i class={link.icon}></i>
        </a>
      ))}
    </div>
    <div class="mt-6 text-sm">
      <p>Where warmth meets flavor in every bite</p>
    </div>
  </div>
</footer>